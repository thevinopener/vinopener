name: frontend
description: "SSAFY 자율프로젝트 C207"
version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.0.1

  # recommend_screen
  carousel_slider: ^4.2.1
  smooth_page_indicator: ^1.1.0

  # Search_Detail_Screen
  dashed_circular_progress_bar: ^0.0.6

  # Search_Camera_Screen
  camera: ^0.10.5+9

  # for gallery image
  image_picker: ^1.1.0

  # for http request
  dio: ^5.4.3+1

  # for nation flag
  flag: ^7.0.0
  flutter_radar_chart: ^0.2.1

  #Note_Color_Screen
  accordion: ^2.6.0

  # for date time
  intl: ^0.17.0

  # firebase
  firebase_core: ^2.30.1
  firebase_auth: ^4.19.4

  # google social login
  google_sign_in: ^6.2.1

  #for rating bar
  custom_rating_bar: ^2.0.5
  flutter_rating_bar: ^4.0.1

  #for AI chat
  floating_chat_button: ^1.0.1
  speech_to_text: ^6.6.1
  flutter_tts: ^3.8.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/aroma/

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter